apiVersion: v1
kind: Service
metadata:
  name: {{ include "express2.fullname" . }}
  labels:
    {{- include "express2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "express2.selectorLabels" . | nindent 4 }}
